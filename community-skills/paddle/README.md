# Paddle

Integrate with Paddle to list, list, list and more.

## Tools

- **List Products** (`paddle_list_products`) — List products in Paddle. Optionally filter by status or tax category. Returns product names, IDs, and statuses.
- **List Prices** (`paddle_list_prices`) — List prices in Paddle. Optionally filter by product or status. Returns price amounts, intervals, and IDs.
- **List Subscriptions** (`paddle_list_subscriptions`) — List subscriptions in Paddle. Optionally filter by status or customer. Returns subscription IDs, statuses, and pricing.
- **List Transactions** (`paddle_list_transactions`) — List transactions in Paddle. Optionally filter by status or subscription. Returns transaction IDs, amounts, and statuses.
- **List Customers** (`paddle_list_customers`) — List customers in Paddle. Optionally filter by email or status. Returns customer names, emails, and IDs.

## Configuration

| Field | Type | Description |
|-------|------|-------------|
| Auth | api_key | paddle authentication |

## Category

finance · Risk: high
